# Database (optional; defaults to sqlite:///./doorbell.db)
# DATABASE_URL=sqlite:///./doorbell.db
# DATABASE_URL=postgresql://user:pass@localhost:5432/knockknock

# Angi webhook auth
ANGI_API_KEY=your-angi-webhook-api-key

# SendGrid (optional; if unset, emails are printed to console)
# SENDGRID_API_KEY=SG.xxx

# OpenAI (optional; only used when USE_LLM_EMAIL=true)
# OPENAI_API_KEY=sk-xxx
# USE_LLM_EMAIL=false
